# 📂 Database Design — JSON Structure Files

⚠️ **Note:** ไฟล์ JSON ทั้งหมดนี้เป็นเพียง **Design Blueprint** สำหรับการพัฒนา (ปัจจุบันข้อมูลจริงถูกเก็บใน **Firebase Firestore** แล้ว)

## File Structure

| ไฟล์ | คำอธิบาย | จำนวน Records |
|------|----------|:---:|
| `edpex_categories.json` | หมวด EdPEx 7.1–7.4 | 4 |
| `departments.json` | หน่วยงานผู้รับผิดชอบ KPI | 6 |
| `staff_users.json` | ผู้ใช้งาน (ตัวอย่าง) | 6 |
| `kpi_master.json` | **รายการ KPI ทั้งหมด** | **61** |
| `kpi_data_academic.json` | ข้อมูล KPI 7.1.x (การศึกษา/วิจัย) | 92 |
| `kpi_data_workforce.json` | ข้อมูล KPI 7.3.x (บุคลากร) | 33 |
| `kpi_data_strategic.json` | ข้อมูล KPI 7.4.x (ยุทธศาสตร์) | 60 |
| `kpi_data_narratives.json` | ข้อมูลเชิงบรรยาย/Milestone | 12 |
| `input_forms.json` | คำจำกัดความฟอร์มกรอก (7 ฟอร์ม) | 7 |
| `input_logs.json` | ตัวอย่าง Audit Trail | 6 |
| `personnel` | (Firestore Only) ข้อมูลบุคลากร | - |
| `students` | (Firestore Only) ข้อมูลนิสิต | - |

## Entity Relationship

```
edpex_categories (4)
  └── kpi_master (61)
        ├── kpi_data_academic (92)    ← ข้อมูลตัวเลข
        ├── kpi_data_workforce (33)   ← ข้อมูลตัวเลข
        ├── kpi_data_strategic (60)   ← ข้อมูลตัวเลข
        └── kpi_data_narratives (12)  ← ข้อมูลบรรยาย

departments (6)
  └── staff_users (6)
        └── input_logs (6)  ← ใครกรอก/อนุมัติเมื่อไร

personnel (Firestore)
students (Firestore)

input_forms (7)  ← ฟอร์มกรอก UI spec
```

## Data Patterns

| Pattern | ตัวอย่าง KPI | รูปแบบ |
|---------|------------|--------|
| **Year-Series** | 7.1.1 สอบผ่านวิชาชีพ | `{ year, value, target }` |
| **Matrix** | 7.1.5 บัณฑิตแยกสาขา | `{ year, dimension, value }` |
| **Survey** | 7.3.10 ผลสำรวจผูกพัน | `{ dimension, score, target }` |
| **Narrative** | 7.1.4 เปรียบเทียบหลักสูตร | `{ title, description, old/new }` |
| **Milestone** | 7.4.13 วัคซีนสัตว์เศรษฐกิจ | `{ title, status, phase }` |

## Input Form Flow

```
Staff กรอกข้อมูล → status: pending_review
     ↓
Admin ตรวจสอบ → status: approved / rejected
     ↓
data_snapshot → บันทึกเข้า kpi_data_*
```
