# คู่มือใช้งาน convert_old_to_template.py

| Field | Value |
|:---|:---|
| **Doc ID** | KUVMIS-DOC-011 |
| **Version** | 1.1b |
| **Standard** | ISO 27001 / EdPEx Compliance |
| **Last Updated** | 2026-02-19T09:52:00+07:00 |
| **Author** | KUVMIS Development Team |
| **Status** | Released |

---

## สารบัญ

1. [Script ทำอะไร](#1-script-ทำอะไร)
2. [ติดตั้ง Python (Windows)](#2-ติดตั้ง-python-windows)
3. [ติดตั้ง Python (macOS)](#3-ติดตั้ง-python-macos)
4. [ติดตั้ง Library](#4-ติดตั้ง-library)
5. [วิธีรัน Script](#5-วิธีรัน-script)
6. [Input ที่รองรับ](#6-input-ที่รองรับ)
7. [Output ที่ได้](#7-output-ที่ได้)
8. [ตัวอย่างการใช้งาน](#8-ตัวอย่างการใช้งาน)
9. [ข้อผิดพลาดที่พบบ่อย](#9-ข้อผิดพลาดที่พบบ่อย)

---

## 1. Script ทำอะไร

แปลงไฟล์ Excel ต้นฉบับ (จาก REG/บัณฑิตวิทยาลัย) ที่มี **6 Sheet** ให้เป็น **Import Template** ที่เว็บระบบ KUVMIS รับได้ (3 Sheets: Students, Publications, Progress)

```
ฐานข้อมูลนิสิต.xlsx (6 Sheets)
        ↓ convert_old_to_template.py
converted_นิสิต_YYYYMMDD_HHMM.xlsx (4 Sheets: Students, Publications, Progress, Advisors)
        ↓
Upload เข้าระบบ KUVMIS → /student > นำเข้า
```

### Sheet ที่ตรวจจับได้

| Sheet ต้นฉบับ | ตรวจจับอัตโนมัติ | แปลงเป็น |
|:---|:---|:---|
| การตีพิมพ์ผลงาน | ✅ มี "ชื่อบทความ" | Publications |
| นิสิตคงอยู่ สำเร็จ | ✅ มี "รหัสนิสิต" (ไทย) | Students |
| นิสิตสละสิทธิ์ ไม่มารายงานตัว | ✅ มี "รหัสนิสิต" (ไทย) | Students |
| รายงานความก้าวหน้า | ✅ มี "STUDENT_ID" (อังกฤษ) | Students + Progress |
| สรุปนิสิตจบแยกตามปีเข้า | ข้ามไป (Pivot สรุป) | — |
| สรุปอาจารย์ที่ปรึกษา | ข้ามไป (ถ้าว่าง) | — |

---

## 2. ติดตั้ง Python (Windows)

### ขั้นตอน

1. เปิด https://www.python.org/downloads/
2. กดปุ่ม **"Download Python 3.x.x"** (ใช้เวอร์ชันล่าสุด)
3. เปิดไฟล์ที่ดาวน์โหลด (.exe)
4. **⚠️ สำคัญ: ติ๊กเครื่องหมาย ✅ "Add Python to PATH"** ด้านล่าง
5. กด **"Install Now"**
6. รอจนเสร็จ → กด Close

### ตรวจสอบ

เปิด **PowerShell** หรือ **Command Prompt** แล้วพิมพ์:

```
python --version
```

ถ้าแสดงเวอร์ชัน (เช่น `Python 3.13.2`) = สำเร็จ

---

## 3. ติดตั้ง Python (macOS)

### วิธีที่ 1: Homebrew (แนะนำ)

เปิด **Terminal** แล้วรัน:

```bash
# ติดตั้ง Homebrew (ถ้ายังไม่มี)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# ติดตั้ง Python
brew install python
```

### วิธีที่ 2: ดาวน์โหลดจากเว็บ

1. เปิด https://www.python.org/downloads/macos/
2. ดาวน์โหลด **macOS Universal Installer** (.pkg)
3. เปิดไฟล์ → ทำตามขั้นตอน → กด Install

### ตรวจสอบ

```bash
python3 --version
```

> **หมายเหตุ:** บน macOS ให้ใช้คำสั่ง `python3` แทน `python`

---

## 4. ติดตั้ง Library

Script ต้องการ library 2 ตัว — ติดตั้ง**ครั้งเดียว**:

### Windows (PowerShell / Command Prompt)

```
pip install pandas openpyxl
```

### macOS (Terminal)

```bash
pip3 install pandas openpyxl
```

ถ้าเจอ `permission denied`:

```bash
pip3 install --user pandas openpyxl
```

---

## 5. วิธีรัน Script

### วิธีที่ 1: รันจาก PowerShell / Terminal (แนะนำ)

**Windows:**

```powershell
python D:\GitHub\MIS\mis-edpex\source\academic\convert_old_to_template.py
```

**macOS:**

```bash
python3 /path/to/mis-edpex/source/academic/convert_old_to_template.py
```

> 💡 สามารถรันจาก directory ไหนก็ได้ — ไม่ต้อง `cd` ไปก่อน

### วิธีที่ 2: cd ไปที่ folder ของ script

```powershell
cd D:\GitHub\MIS\mis-edpex\source\academic
python convert_old_to_template.py
```

### วิธีที่ 3: Double-click (Windows เท่านั้น)

1. เปิด File Explorer
2. ไปที่ `D:\GitHub\MIS\mis-edpex\source\academic\`
3. **Double-click** ไฟล์ `convert_old_to_template.py`
4. หน้าต่างดำจะเปิดขึ้น → ลากไฟล์ Excel มาวาง → กด Enter
5. รอจนเสร็จ → กด Enter เพื่อปิด

> ⚠️ ถ้า Double-click แล้วหน้าต่างปิดทันที = ยังไม่ได้ติดตั้ง library (ดูขั้นตอนที่ 4)

---

## 6. Input ที่รองรับ

| ประเภท | รองรับ |
|:---|:---|
| `.xlsx` (Excel) | ✅ อ่านทุก Sheet อัตโนมัติ |
| `.xls` (Excel เก่า) | ✅ (ต้องติดตั้ง `xlrd`: `pip install xlrd`) |
| `.csv` (CSV) | ✅ อ่านเป็น 1 Sheet |
| หลายไฟล์พร้อมกัน | ✅ คั่นด้วย space |

### ตัวอย่าง Input

- `C:\Users\ชื่อ\Downloads\ฐานข้อมูลนิสิต.xlsx` — ไฟล์ต้นฉบับจาก REG (6 Sheets)
- `C:\Users\ชื่อ\Downloads\ฐานข้อมูลนิสิต_clean.xlsx` — ไฟล์ที่ทำความสะอาดแล้ว (6 Sheets)
- CSV แยกรายไฟล์ (`C:\Users\ชื่อ\Downloads\การตีพิมพ์ผลงาน.csv` ฯลฯ)

---

## 7. Output ที่ได้

### ชื่อไฟล์

```
converted_นิสิต_YYYYMMDD_HHMM.xlsx
```

ตัวอย่าง: `converted_นิสิต_20260219_0951.xlsx`

### ตำแหน่งไฟล์

**ไฟล์ output จะถูกบันทึกที่ folder เดียวกับ Input**

- ถ้า Input อยู่ที่ `C:\Users\ชื่อ\Downloads\` → Output จะอยู่ที่ `C:\Users\ชื่อ\Downloads\`

### โครงสร้าง Excel (4 Sheets)

| Sheet | เนื้อหา | ข้อมูลที่แปลง |
|:---|:---|:---|
| **Students** | ข้อมูลนิสิตทั้งหมด (dedup by student_id) | นิสิตคงอยู่ + สละสิทธิ์ + รายงานความก้าวหน้า |
| **Publications** | ผลงานตีพิมพ์ | การตีพิมพ์ผลงาน |
| **Progress** | Milestone (แปลง Wide → Long) | รายงานความก้าวหน้า (9 milestones) |
| **Advisors** | อาจารย์ที่ปรึกษา (dedup by ชื่อ) | ดึงจากข้อมูลนิสิต (ชื่อ, ภาควิชา, TEACHER_CARD) |

### Milestone ที่แปลงจาก รายงานความก้าวหน้า

| Milestone | Status Column | Date Column |
|:---|:---|:---|
| Proposal | PROPOSAL_SUBMIT | PROP_DATE |
| English | ENGEXAM_STATUS | ENGEXAM_DATE |
| StudyPlan | STUDYPLAN_SUBMIT | STUDYPLAN_DATE |
| ComprehensiveWriting | COMPREHENSIVE_WRITING_STATUS | COMPRE_WRITING_DATE |
| ComprehensiveOral | COMPREHENSIVE_ORAL_STATUS | COMPRE_ORAL_DATE |
| QualifyWriting | QUALIFYEXAM_WRITING_STATUS | QUALIFY_WRITING_DATE |
| QualifyOral | QUALIFYEXAM_ORAL_STATUS | QUALIFY_ORAL_DATE |
| Defense | DEFENDEXAM_STATUS | DEFEND_DATE |
| Manuscript | MANUSCRIPT_STATUS | MANUSCRIPT_DATE |

---

## 8. ตัวอย่างการใช้งาน

### Windows — ใช้ไฟล์เดียว

```powershell
PS C:\Users\ชื่อ> python convert_old_to_template.py

==============================================
  แปลง Excel/CSV ต้นฉบับ → Import Template
==============================================
รองรับ: CSV 1-4, 6 (ข้าม CSV 5 ที่เป็นสรุป)

ลากไฟล์ Excel/CSV มาวาง (หลายไฟล์คั่นด้วย space) แล้วกด Enter:
> "C:\Users\ชื่อ\Downloads\ฐานข้อมูลนิสิต.xlsx"

กำลังวิเคราะห์ไฟล์: ฐานข้อมูลนิสิต.xlsx

  Sheet: 'นิสิตคงอยู่ สำเร็จ' — 391 แถว, 30 คอลัมน์
  > ตรวจพบ: ข้อมูลนิสิต (Student Profile)

  Sheet: 'รายงานความก้าวหน้า' — 163 แถว, 56 คอลัมน์
  > ตรวจพบ: รายงานความก้าวหน้า (Progress Report)

  Sheet: 'การตีพิมพ์ผลงาน' — 135 แถว, 18 คอลัมน์
  > ตรวจพบ: การตีพิมพ์ผลงาน (Publications)

==============================================
  ผลลัพธ์: 427 นิสิต, 99 ผลงาน, 775 milestone
  > Sheet Students: 427 แถว
  > Sheet Publications: 99 แถว
  > Sheet Progress: 775 แถว

  > ไฟล์ผลลัพธ์: C:\Users\ชื่อ\Downloads\converted_นิสิต_20260219_0951.xlsx
```

### macOS — ใช้ไฟล์เดียว

```bash
$ python3 convert_old_to_template.py

ลากไฟล์ Excel/CSV มาวาง แล้วกด Enter:
> /Users/ชื่อ/Downloads/ฐานข้อมูลนิสิต.xlsx
```

### ใช้หลายไฟล์ CSV พร้อมกัน

```powershell
> "C:\Users\ชื่อ\Downloads\การตีพิมพ์ผลงาน.csv" "C:\Users\ชื่อ\Downloads\นิสิตคงอยู่.csv" "C:\Users\ชื่อ\Downloads\รายงานความก้าวหน้า.csv"
```

### ขั้นตอนหลังแปลง

1. เปิดไฟล์ `converted_นิสิต_YYYYMMDD_HHMM.xlsx` ตรวจดูข้อมูล
2. เข้าเว็บ KUVMIS → **Student > นำเข้า ▾ > เลือกไฟล์ Import**
3. เลือกไฟล์ที่แปลงแล้ว → ระบบจะตรวจซ้ำอัตโนมัติ
4. ตรวจสอบสรุปผลการนำเข้า (เพิ่ม/อัปเดต/ข้าม)

---

## 9. ข้อผิดพลาดที่พบบ่อย

| ปัญหา | สาเหตุ | วิธีแก้ |
|:---|:---|:---|
| `'python' is not recognized` | ไม่ได้ติ๊ก "Add to PATH" ตอนติดตั้ง | ติดตั้ง Python ใหม่ + ติ๊ก "Add Python to PATH" |
| `No module named 'pandas'` | ยังไม่ได้ติดตั้ง library | รัน `pip install pandas openpyxl` |
| `No module named 'openpyxl'` | ยังไม่ได้ติดตั้ง library | รัน `pip install openpyxl` |
| Double-click แล้วปิดทันที | เกิด error แต่หน้าต่างปิดเร็ว | ให้รันจาก Terminal/PowerShell แทน จะเห็น error |
| `IndexError: list index out of range` | Sheet ว่างเปล่า (0 คอลัมน์) | อัปเดต script เวอร์ชันล่าสุด (แก้แล้ว) |
| `[Error] หาไฟล์ไม่พบ` | Path ไม่ถูกต้อง | ลากไฟล์จาก File Explorer มาวางใน Terminal |
| `⚠️ ไม่พบข้อมูลที่แปลงได้` | Header ไม่ตรงกับที่ script คาดหวัง | ตรวจสอบว่าใช้ไฟล์ต้นฉบับจาก REG/บัณฑิต |
| `pip: command not found` (macOS) | macOS ใช้ pip3 | เปลี่ยนเป็น `pip3 install pandas openpyxl` |
