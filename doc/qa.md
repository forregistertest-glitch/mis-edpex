# การรับรองความถูกต้องของข้อมูล (Data QA & Authenticity)

| Field | Value |
|:------|:------|
| **Doc ID** | KUVMIS-DOC-007 |
| **Version** | 1.3.0 |
| **Last Updated** | 2026-02-11T01:34:00+07:00 |
| **Author** | KUVMIS Development Team |
| **Status** | Released |

เอกสารฉบับนี้อธิบายถึงที่มาและความถูกต้องของข้อมูลภายในระบบ KUVMIS เพื่อใช้ประกอบการตัดสินใจในช่วงการทดสอบระบบ (Pilot Test)

---

## 1. ข้อมูลหลัก (KPI Master Data)

**สถานะ:** ✅ **ถูกต้องตามเอกสาร 100%**

ข้อมูลโครงสร้างทั้งหมดถูกนำเข้าจากไฟล์นิยามตัวชี้วัด (`kpi_master.json`) ประกอบด้วยตัวชี้วัดทั้งหมด **61 รายการ** โดยดึงข้อมูลฟิลด์ต่างๆ ดังนี้:
- **รหัสตัวชี้วัด (KPI ID):** ตรงตามมาตรฐานโครงการ (เช่น 7.1.1, 7.4.15)
- **ชื่อตัวชี้วัด:** ทั้งภาษาไทยและภาษาอังกฤษ ตรงตามเอกสารต้นฉบับ
- **หน่วยนับ (Unit):** เช่น ร้อยละ, ราย, บาท, ครั้ง
- **ค่าเป้าหมาย (Target Value):** ดึงจากเกณฑ์การตัดสินในเอกสาร
- **การจัดกลุ่ม (Category):** แยกตามหมวดหมู่ด้านวิชาการ, ทรัพยากรบุคคล, โรงพยาบาล และยุทธศาสตร์

---

## 2. ข้อมูลผลการดำเนินงาน (Performance Data / Seed Data)

**สถานะ:** 🟡 **ข้อมูลจำลองเพื่อการทดสอบ (Mock Data)**

เนื่องจากระบบอยู่ในช่วงเริ่มต้นพัฒนา ข้อมูลตัวเลขผลการดำเนินงานที่ปรากฏหลังจากกดปุ่ม "Seed Database" จึงเป็นข้อมูลที่ระบบสร้างขึ้นมาเพื่อจำลองสถานการณ์ (Simulated Data) โดยมีรายละเอียดดังนี้:

| ประเภทข้อมูล | แหล่งที่มา | วัตถุประสงค์ |
|-------------|----------|-------------|
| **ค่าตัวเลข (Values)** | ระบบสุ่มแบบมีเงื่อนไข | เพื่อทดสอบการคำนวณและแสดงผลกราฟ |
| **ข้อมูลย้อนหลัง 5 ปี** | ข้อมูลสมมติ (Trend Line) | เพื่อแสดงแนวโน้ม (Trend) การเติบโต |
| **ข้อมูลรายไตรมาส** | ข้อมูลสมมติ | เพื่อทดสอบระบบ Period Selector |
| **ข้อมูลแยกสาขา/มิติ** | รายชื่อสาขาจริง / ตัวเลขสมมติ | เพื่อทดสอบการแสดงผลแบบ Matrix |

---

## 3. วิธีการตรวจสอบความเที่ยงตรง (Verification Methods)

ผู้ดูแลระบบสามารถตรวจสอบความถูกต้องของระบบการจัดการข้อมูลได้ผ่านช่องทางดังนี้:

1. **จำนวนรายการ:** ตรวจสอบจาก Dashboard (Hero Section) ต้องแสดงรายการ KPI ครบ 61 รายการ
2. **การคำนวณ:** เปรียบเทียบค่าใน Dashboard กับข้อมูลดิบในเมนู "รายงาน" (Reports) ซึ่งสามารถส่งออกเป็น Excel เพื่อตรวจทานได้
3. **การเปรียบเทียบเป้าหมาย:** ระบบจะแสดงสี (เขียว/แดง) โดยคำนวณจากค่าจริงเปรียบเทียบกับค่าเป้าหมายที่ระบุไว้ในเอกสารนิยาม

---

## 4. แนวทางการจัดการข้อมูลจริง (Transition to Real Data)

เมื่อสิ้นสุดช่วงการพรีเซ็นต์หรือการทดสอบ และต้องการเริ่มใช้งานข้อมูลจริงของคณะ:
1. Admin เข้าสู่ระบบด้วย Google Account → ไปเมนู **"จัดการผู้ใช้"** เพิ่มรายชื่อผู้ใช้ทั้งหมด พร้อมกำหนด Role
2. Admin ใช้เมนู **Seed Tool** เพื่อล้างข้อมูลจำลองทั้งหมด
3. เจ้าหน้าที่ (Role: user) Login ด้วย Google Account → เข้าเมนู **"กรอกข้อมูล"** (Input Data) เพื่อนำเข้าข้อมูลจริงตามรอบปีงบประมาณ
4. Reviewer/Admin ตรวจสอบและ **Approve** ข้อมูลที่กรอก → ข้อมูลที่อนุมัติแล้วจะถูกแสดงใน Dashboard
5. ระบบจะเริ่มประมวลผลจากข้อมูลจริงที่ถูกบันทึกลงในฐานข้อมูล Cloud Firestore ทันที

---
*เอกสารฉบับนี้ปรับปรุงล่าสุดเมื่อ 11 ก.พ. 2569 — KUVMIS v1.2*
