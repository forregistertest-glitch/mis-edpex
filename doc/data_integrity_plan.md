# แผนการรักษาความถูกต้องและความน่าเชื่อถือของข้อมูล (Data Integrity Assurance Plan)
# คณะสัตวแพทยศาสตร์ มหาวิทยาลัยเกษตรศาสตร์

| Field | Value |
|:------|:------|
| **Doc ID** | KUVMIS-DOC-010 |
| **Version** | 1.02d |
| **Standard** | ISO 27001 / EdPEx Compliance |
| **Last Updated** | 2026-02-16T19:25:00+07:00 |
| **Author** | KUVMIS Development Team |
| **Status** | Released |

---

เอกสารฉบับนี้จัดทำขึ้นเพื่อกำหนดแนวทางปฏิบัติและมาตรฐานในการรักษาความถูกต้อง (Accuracy) ความสมบูรณ์ (Completeness) และความน่าเชื่อถือ (Reliability) ของข้อมูลในระบบสารสนเทศเพื่อการจัดการ (MIS) เพื่อให้สอดคล้องกับมาตรฐานการประกันคุณภาพการศึกษา (EdPEx) และมาตรฐานสากลด้านความปลอดภัยสารสนเทศ (ISO 27001)

---

## 1. หลักการสำคัญ (Core Principles) — ALCOA+ Standard

ระบบ MIS ยึดถือหลักปฏิบัติสากล **ALCOA+** ซึ่งเป็นมาตรฐานที่ใช้กันแพร่หลายในอุตสาหกรรมยาและการเงิน เพื่อรับรองความน่าเชื่อถือของข้อมูล:

1.  **A - Attributable (ระบุตัวตนได้):** ทุกการกระทำกับข้อมูลต้องระบุได้ว่า "ใคร" เป็นคนทำ (Who) และทำเมื่อไหร่ (When)
2.  **L - Legible (อ่านออกและถาวร):** ข้อมูลต้องอ่านเข้าใจได้ และคงอยู่ตลอดอายุการเก็บรักษา
3.  **C - Contemporaneous (เป็นปัจจุบัน):** การบันทึกข้อมูลต้องทำทันทีเมื่อเกิดเหตุการณ์ (Real-time logging)
4.  **O - Original (ต้นฉบับ):** ต้องรักษาข้อมูลต้นฉบับไว้ หากมีการแก้ไขต้องเก็บประวัติ (Audit Trail)
5.  **A - Accurate (ถูกต้อง):** ข้อมูลต้องถูกต้องตรงตามความเป็นจริง ผ่านการตรวจสอบ (Validation)

---

## 2. นโยบายการจำแนกและจัดการข้อมูล (Data Classification & Management Policy)

เพื่อให้ข้อมูลมีความน่าเชื่อถือสูงสุด ระบบได้แบ่งแยกประเภทข้อมูลและการจัดการดังนี้:

### 2.1 การแยกแวดล้อมระบบ (Environment Separation) 🏗️

การปะปนกันของ "ข้อมูลจริง" และ "ข้อมูลทดสอบ" เป็นสาเหตุหลักที่ทำให้รายงานขาดความน่าเชื่อถือ ดังนั้นจึงกำหนดนโยบายเคร่งครัด:

| Environment | วัตถุประสงค์ | นโยบายข้อมูล (Data Policy) | เครื่องมือ Mock Data |
| :--- | :--- | :--- | :--- |
| **Development** (Local) | สำหรับพัฒนาและทดสอบระบบ | อนุญาตให้สร้าง/ลบข้อมูลจำลองได้อิสระ | ✅ **เปิดใช้งาน** |
| **Staging / UAT** | สำหรับให้ User ทดสอบก่อนขึ้นจริง | ใช้ข้อมูลจำลองเสมือนจริง (Anonymized Data) | ✅ **เปิดใช้งาน** (จำกัดกลุ่ม) |
| **Production** (Live) | **สำหรับใช้งานจริงเท่านั้น** | **ห้าม** มีข้อมูลจำลองปะปนเด็ดขาด | ❌ **ปิดถาวร (Disabled)** |

> **[!] มาตรการป้องกัน:** ในระดับ Source Code ระบบจะตรวจสอบ Environment Variable (`NODE_ENV`) หากเป็น `production` ฟังก์ชันการสร้าง/ลบ Mock Data จะถูกตัดการทำงานออกจากระบบโดยสิ้นเชิง (Code Elimination) ทำให้ไม่สามารถเรียกใช้ได้แม้จะทราบช่องทาง

### 2.2 การระบุแหล่งที่มาของข้อมูล (Source Tracing) 🏷️

ในกรณีที่มีความจำเป็นต้องนำเข้าข้อมูลจากแหล่งอื่น ต้องมีการระบุ Origin ของข้อมูลชัดเจนในระดับ Database:

*   **System Generated:** ข้อมูลที่เกิดจากการประมวลผลของระบบ
*   **Manual Input:** ข้อมูลที่เกิดจากการกรอกโดยเจ้าหน้าที่ (ต้องระบุ User ID)
*   **External Interface:** ข้อมูลที่นำเข้าจากระบบอื่น (เช่น Reg, ERP)
*   *(Test Data Flag):* หากมีการทดสอบในระบบจริง ข้อมูลทดสอบต้องถูกระบุ Flag `is_test_data = true` และถูกกรองออกจากรายงานทุกฉบับโดยอัตโนมัติ

---

## 3. ระบบติดตามตรวจสอบ (Audit Trails) 📝

เพื่อความโปร่งใสและตรวจสอบย้อนหลังได้ (Traceability) ระบบต้องบันทึกประวัติกิจกรรมสำคัญ (Critical Actions) ดังนี้:

### 3.1 ขอบเขตการบันทึก (Scope)
*   การเข้าสู่ระบบ (Login/Logout) และการยืนยันตัวตนที่ล้มเหลว
*   การเพิ่ม/แก้ไข/ลบ ข้อมูลระดับ Master Data (เช่น เกณฑ์ KPI, ค่าเป้าหมาย)
*   การอนุมัติหรือปฏิเสธข้อมูล (Approve/Reject)
*   การแก้ไขข้อมูลย้อนหลัง (Retrospective Edit)

### 3.2 องค์ประกอบของบันทึก (Log Content)
ทุก Audit Log ต้องประกอบด้วย:
1.  **Timestamp:** วัน-เวลาที่เกิดเหตุการณ์ (UTC หรือ Server Time ที่เชื่อถือได้)
2.  **User ID:** รหัสผู้ใช้งานที่กระทำการ
3.  **Action:** ประเภทการกระทำ (Create, Update, Delete)
4.  **Target Object:** ข้อมูลชุดไหนที่ถูกกระทำ (เช่น KPI-7.1.1)
5.  **Change Detail:** ค่าเดิม (Old Value) และ ค่าใหม่ (New Value) *[เพื่อดูความเปลี่ยนแปลง]*
6.  **IP Address / Location:** แหล่งที่มาของการเชื่อมต่อ

---

## 4. นโยบายการลบข้อมูล (Deletion Policy) 🗑️

เพื่อให้สอดคล้องกับหลักธรรมาภิบาลข้อมูล (Data Governance) ระบบใช้กลไก **Soft Delete** แทนการลบถาวร:

*   **Soft Delete:** เมื่อผู้ใช้สั่ง "ลบ" ระบบจะเพียงแค่ทำเครื่องหมาย (Flag) ว่าข้อมูลนั้นถูกยกเลิก (`deleted_at = timestamp`) ข้อมูลจะหายไปจากหน้าจอผู้ใช้ แต่ยังคงอยู่ในฐานข้อมูลสำหรับการตรวจสอบ
*   **Hard Delete:** การลบข้อมูลถาวรออกจากสื่อบันทึก จะทำได้เฉพาะกรณี:
    *   ข้อมูลขยะจากการทดสอบระบบ (Cleansing)
    *   หมดอายุการจัดเก็บตามกฎหมาย (Retention Period)
    *   คำสั่งศาลหรือกฎหมาย PDPA (Right to be forgotten)
    *   *ต้องกระทำโดย Database Administrator (DBA) ระดับสูงเท่านั้น*

---

## 5. การอ้างอิงและมาตรฐานเทียบเคียง (Industry References)

แนวทางปฏิบัตินี้อ้างอิงจากมาตรฐานของระบบ Enterprise Resource Planning (ERP) ชั้นนำ:

*   **SAP S/4HANA:** ใช้หลักการ "Document Principle" คือเมื่อบันทึกรายการแล้ว จะแก้ไขไม่ได้ ต้องสร้างรายการใหม่เพื่อกลับรายการ (Reverse) เท่านั้น เพื่อให้เกิด Audit Trail ที่สมบูรณ์
*   **Salesforce:** มีระบบ "Field History Tracking" เพื่อติดตามการเปลี่ยนแปลงข้อมูลสำคัญ และแยก Sandbox Environment สำหรับทดสอบอย่างชัดเจน
*   **ISO/IEC 27001:** มาตรฐานความมั่นคงปลอดภัยสารสนเทศ ข้อกำหนด A.12.1.4 (Separation of development, testing and operational environments) และ A.12.4 (Logging and monitoring)

---

## 6. บทสรุป (Conclusion)

การดำเนินการตามแผน Data Integrity Assurance Plan นี้ จะช่วยให้คณะฯ มั่นใจได้ว่าสารสนเทศที่นำเสนอในรายงาน SAR (Self-Assessment Report) และรายงานประจำปี มีความถูกต้อง น่าเชื่อถือ ปราศจากข้อมูลขยะ และสามารถตรวจสอบย้อนหลังได้ทุกขั้นตอน ซึ่งเป็นพื้นฐานสำคัญของการบริหารงานคุณภาพสู่ความเป็นเลิศ (EdPEx)
